import 'package:flutter/material.dart';

class CidadeCustomWidget extends StatelessWidget {

  const CidadeCustomWidget({ Key? key }) : super(key: key);

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text(''),),
           body: Container(),
       );
  }
}